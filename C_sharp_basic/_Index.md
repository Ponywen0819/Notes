#note

# C# Basic 索引

依學習順序組織的 C# 基礎主題目錄。

## 入門基礎
- [[C sharp program structure]] —— 命名空間、Main vs Top-level statements、進入點
- [[C sharp type system]] —— 值類型 vs 引用類型、裝箱/拆箱、`var`/`dynamic`/`object`
- [[C sharp variables and constants]] —— 變數宣告、`const`/`readonly`、作用域
- [[C sharp operators]] —— 算術、邏輯、位元、`is`/`as`/`??`/`?.`
- [[C sharp string]] —— 字串不可變性、`StringBuilder`、字串內插、格式化

## 流程控制
- [[C sharp control flow]] —— `if`/`else`/`switch`、pattern matching
- [[C sharp loops]] —— `for`/`while`/`do-while`/`foreach`、`break`/`continue`

## 資料結構
- [[C sharp arrays]] —— 一維/多維/鋸齒陣列、`Array` 常用方法
- [[C sharp collections]] —— `List`、`Dictionary`、`HashSet`、`Queue`、`Stack`

## 方法與函數
- [[C sharp methods]] —— 參數傳遞、預設參數、命名參數、方法重載
- [[C sharp delegates and events]] —— `Action`、`Func`、`Predicate`、event 機制
- [[C sharp lambda expressions]] —— Lambda 語法、closure

## 物件導向
- [[C sharp class and object]] —— 欄位、屬性、建構式、靜態成員
- [[C sharp inheritance]] —— `: base`、`virtual`/`override`/`new`/`sealed`
- [[C sharp polymorphism]] —— 多型、向上/向下轉型、`is`/`as` pattern
- [[C sharp interface]] —— 介面定義、明確實作、預設介面方法
- [[C sharp abstract class]] —— 抽象類別 vs 介面
- [[C sharp struct and record]] —— `struct`、`record`、`record struct`、不可變物件

## 進階型別
- [[C sharp generics]] —— 泛型類別/方法、型別約束
- [[C sharp enum]] —— 列舉、`[Flags]`、`Enum` 類別
- [[C sharp nullable types]] —— `T?`、nullable reference types、`!` 寬容運算子

## 錯誤處理 & 進階特性
- [[C sharp exception handling]] —— `try`/`catch`/`finally`、自訂例外、`using`
- [[C sharp LINQ]] —— 查詢語法、方法語法、deferred execution
- [[C sharp async await]] —— `Task`、`async`/`await`、取消權杖
- [[C sharp attributes and reflection]] —— `[Attribute]`、`typeof`、`Type`

## 周邊知識
- [[C sharp namespace and using]] —— `using` directive、`global using`
- [[C sharp file IO]] —— `File`、`Stream`、async I/O
- [[C sharp project structure]] —— `.csproj`、NuGet、`dotnet` CLI
