#note

# `for` 迴圈

# `while` 迴圈

# `do-while` 迴圈

# `foreach` 迴圈

## 與 `IEnumerable<T>` 的關係

## 編譯器如何展開 `foreach`

# 控制流程

## `break`

## `continue`

## `return` 在迴圈中的行為

## `goto`（不建議）

# 無限迴圈

# 巢狀迴圈

# 效能比較
- `for` vs `foreach`
- 對陣列、`List<T>`、`Dictionary` 的差異
