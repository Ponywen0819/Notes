#note

# 類別 vs 物件

# 類別宣告

## 存取修飾子 (`public` / `private` / `protected` / `internal`)

## 巢狀類別

## `partial class`

# 欄位 (Field)

# 屬性 (Property)

## `get` / `set`

## 自動屬性 (Auto-property)

## `init` 存取子 (C# 9+)

## 唯讀屬性

## 屬性初始化

# 索引器 (Indexer) `this[int i]`

# 建構式 (Constructor)

## 預設建構式

## 參數化建構式

## 建構式鏈 (`: this(...)`)

## 主要建構式 (Primary constructor, C# 12+)

# 解構式 (Finalizer) `~ClassName()`

# `this` 關鍵字

# 靜態成員 (`static`)

## 靜態欄位 / 屬性 / 方法

## 靜態建構式

## `static class`

# Object Initializer 與 Collection Initializer

# `new` 運算子與目標型別 `new()`
