#note

# `System.Collections.Generic` 概覽

# `List<T>`
- 常用方法：`Add`、`Remove`、`Insert`、`Contains`、`IndexOf`、`Sort`
- 容量 vs 數量 (`Capacity` vs `Count`)

# `Dictionary<TKey, TValue>`
- 常用方法：`Add`、`Remove`、`ContainsKey`、`TryGetValue`
- 雜湊衝突與效能

# `HashSet<T>`
- 集合運算：`UnionWith`、`IntersectWith`、`ExceptWith`

# `Queue<T>` / `Stack<T>`
- FIFO vs LIFO
- `Enqueue`/`Dequeue` vs `Push`/`Pop`

# `LinkedList<T>`

# `SortedList<TKey, TValue>` / `SortedDictionary<TKey, TValue>` / `SortedSet<T>`

# `IEnumerable<T>` vs `ICollection<T>` vs `IList<T>` 介面層次

# 唯讀集合
`ReadOnlyCollection<T>`、`IReadOnlyList<T>`

# 並行集合 (`System.Collections.Concurrent`)

# 集合初始化器與集合運算式 (C# 12+)
