#note

# `System.IO` 概覽

# 檔案路徑

## `Path` 類別

## 跨平台路徑分隔符

## 絕對路徑 vs 相對路徑

# `File` 與 `FileInfo`

## 建立 / 刪除 / 複製 / 移動

## 檢查存在 (`File.Exists`)

## 讀寫文字（`ReadAllText` / `WriteAllText` / `ReadAllLines`）

## 讀寫位元組

# `Directory` 與 `DirectoryInfo`

## 建立 / 刪除目錄

## 列舉檔案與子目錄

# `Stream` 階層

## `FileStream`

## `MemoryStream`

## `BufferedStream`

# 文字讀寫

## `StreamReader` / `StreamWriter`

## 編碼 (`Encoding`)

# 二進位讀寫

## `BinaryReader` / `BinaryWriter`

# 非同步 I/O
`ReadAsync`、`WriteAsync`、`File.ReadAllTextAsync`

# 序列化
- JSON (`System.Text.Json`)
- XML (`System.Xml.Serialization`)

# 資源釋放與 `using`
