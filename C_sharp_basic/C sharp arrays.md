#note

# 陣列基礎

# 一維陣列

## 宣告與初始化

## 存取與修改

# 多維陣列 (Rectangular Array)
`int[,] matrix = new int[3, 4];`

# 鋸齒陣列 (Jagged Array)
`int[][] jagged = new int[3][];`

# 多維 vs 鋸齒比較

# `Array` 類別常用方法
`Length`、`Sort`、`Reverse`、`IndexOf`、`Copy`、`Clear`、`Resize`

# 陣列與 `IEnumerable<T>`

# `Span<T>` / `Memory<T>` 簡介

# 常見陷阱
- 越界存取 (`IndexOutOfRangeException`)
- 引用共享問題
