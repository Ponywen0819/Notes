#note

# Lambda 語法

## Expression lambda `x => x + 1`

## Statement lambda `x => { ... }`

## 多參數 `(x, y) => x + y`

## 無參數 `() => 42`

# 與 delegate / `Func` / `Action` 的關係

# 型別推斷

# Closure（閉包）

## 捕捉外部變數

## 捕捉變數的生命週期

## 常見陷阱（迴圈變數）

# Lambda 與 LINQ 的搭配

# Expression Tree (`Expression<TDelegate>`)

# 靜態 Lambda (`static =>`，C# 9+)
