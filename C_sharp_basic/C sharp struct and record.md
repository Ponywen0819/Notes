#note

# `struct`（值類型）

## 語法

## 與 `class` 的差異

## 預設建構式限制

## 何時使用 `struct`

## `readonly struct`

## `ref struct`（如 `Span<T>`）

# `record`（C# 9+）

## 為什麼需要 record

## 語法（位置式 / 名義式）

## 值相等性 (Value Equality)

## 不可變性 (Immutability)

## `with` 表達式（非破壞性變更）

## 解構 (Deconstruction)

# `record struct`（C# 10+）

# `class` vs `struct` vs `record` vs `record struct` 比較表

# 適用情境
