#note

# `enum` 基礎

## 語法

## 預設底層型別 (`int`)

## 自訂底層型別 (`enum E : byte`)

## 指定數值

# 列舉與整數的轉換

# `Enum` 類別常用方法

## `Enum.Parse` / `TryParse`

## `Enum.GetValues` / `GetNames`

## `Enum.IsDefined`

# `[Flags]` 屬性

## 位元組合

## 位元運算子搭配

## `HasFlag` 方法

# 列舉的字串輸出

# 列舉與 `switch`
